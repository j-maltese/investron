# === Investron Frontend — Local Development ===
# Copy this file to .env:  cp .env.example .env

# --- API ---
# Leave empty when using Vite proxy (default). The proxy in vite.config.ts
# forwards /api and /auth to localhost:8000 automatically.
VITE_API_BASE_URL=

# --- Supabase (NOT needed for local dev when backend DEBUG=true) ---
# Leave empty for local dev — the frontend will skip Supabase auth
# and the backend returns a dev user automatically.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
