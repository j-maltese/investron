# Generated datasets — large, reproducible from build_dataset.py. Don't commit.
data/
# MLflow local tracking store (Phase 2).
mlruns/
# Trained model artifacts — reproducible from train.py; the chosen one gets copied
# into the backend deliberately in Phase 3.
models/
# Python / notebook cruft.
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
